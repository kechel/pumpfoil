<?xml version="1.0" encoding="utf-8"?>
<!-- NUR Debug-Builds: erlaubt Klartext-HTTP (Emulator gegen lokalen Server 10.0.2.2:8090).
     Wird von Gradle ausschliesslich in den Debug-Build gemerged; das Release-Manifest
     (src/main) bleibt HTTPS-only. Committen ist unbedenklich — betrifft nie Produktion. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />
</manifest>
